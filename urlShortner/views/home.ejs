<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL-Shortner</title>
</head>
<body>
    <div class="container">
       <h1>Welcome To URL-Shortner</h1>
       <div class="url-generated">
       <% if (locals.id){ %>
        <p> URL Generated : http://localhost:2003/url/<%= id %></p>
      <% } %>
       </div>
       <div class="sub-container">
       <form action="/url" method="POST">
        <label for="Url-input">Enter Your URL :-</label>
        <input type="text" placeholder="https://www.example.com" id="Url-input" name="url">
        <button type="submit">Create</button>
    </form>
</div>
<div class="all-shorturl">
    <% if(locals.urls){ %>
<table>
    <thead>
        <th>S. no.</th>
        <th>ShortId</th>
        <th>Redirect</th>
        <th>Clicks</th>
    </thead>
    <tbody>
       <% urls.forEach((url , index )=>{ %>
        <tr>
        <td><%= index + 1 %></td>
        <td><%= url.shortId %></td>
        <td><%= url. redirectUrl %></td>
        <td><%= url.visitHistory.length %></td>
    </tr>
      <% }); %>
    </tbody>
</table>
    <% } %>
</div>
    </div>
</body>
</html>